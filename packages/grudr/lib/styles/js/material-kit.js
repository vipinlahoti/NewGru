$(document).ready(function() {
  //    Activate bootstrap-select
  $('select').selectpicker();
  
  // if ($('input[name="email"]').val().length !== 0) {
  //   $('input[name="email"]').prop('disabled', true);
  // }

  // if ($('input[name="professionalLicenseNumber"]').val().length !== 0) {
  //   $('input[name="professionalLicenseNumber"]').prop('disabled', true);
  // }
});

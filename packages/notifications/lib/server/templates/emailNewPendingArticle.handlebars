<span class="heading">
<a href="{{profileUrl}}">{{articleAuthorName}}</a>
has a new article pending approval:
{{#if url}}
  <a href="{{linkUrl}}" class="action-link">{{articleTitle}}}</a>
{{else}}
  {{articleTitle}}}
{{/if}}
</span><br><br>

{{#if htmlBody}}
  <div class="article-body">
  {{{htmlBody}}}
  </div>
  <br>
{{/if}}

<a href="{{articleUrl}}">Go to article</a><br><br>

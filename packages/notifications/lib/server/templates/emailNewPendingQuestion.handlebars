<span class="heading">
<a href="{{profileUrl}}">{{questionAuthorName}}</a>
has a new question pending approval:
{{#if url}}
  <a href="{{linkUrl}}" class="action-link">{{questionExcerpt}}}</a>
{{else}}
  {{questionExcerpt}}}
{{/if}}
</span><br><br>

{{#if htmlBody}}
  <div class="question-body">
  {{{htmlBody}}}
  </div>
  <br>
{{/if}}

<a href="{{questionUrl}}">Go to question</a><br><br>

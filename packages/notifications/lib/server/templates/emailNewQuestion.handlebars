<span class="heading">
<a href="{{profileUrl}}">{{questionAuthorName}}</a>
has created a new question:
{{#if url}}
  <a href="{{linkUrl}}" class="action-link">{{questionExcerpt}}</a>
{{else}}
  {{questionExcerpt}}
{{/if}}
</span><br><br>

{{#if htmlBody}}
  <div class="question-body">
  {{{htmlBody}}}
  </div>
  <br>
{{/if}}

<a href="{{questionUrl}}">Discuss</a><br><br>
